package com.hx.ncccu;

import java.util.Scanner;

/**
 * @author jxlgcmh
 * @date 2019-11-17 14:46
 * @description
 */
public class Main8 {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        String line = input.nextLine();

    }
    public static String resolve(String str) {

        if (str.contains("aa")) {
            return str.replace("aa", "");
        }
        if (str.contains("bb")) {
            return str.replace("aa", "");
        }
        if (str.contains("bb")) {
            return str.replace("bb", "");
        }if (str.contains("dd")) {
            return str.replace("dd", "");
        }if (str.contains("ee")) {
            return str.replace("ee", "");
        }
        if (str.contains("ff")) {
            return str.replace("ff", "");
        }
        if (str.contains("gg")) {
            return str.replace("gg", "");
        }
        if (str.contains("hh")) {
            return str.replace("hh", "");
        }if (str.contains("ii")) {
            return str.replace("ii", "");
        }if (str.contains("jj")) {
            return str.replace("jj", "");
        }if (str.contains("kk")) {
            return str.replace("kk", "");
        }
        if (str.contains("ll")) {
            return str.replace("ll", "");
        }
        if (str.contains("mm")) {
            return str.replace("mm", "");
        }if (str.contains("nn")) {
            return str.replace("nn", "");
        }if (str.contains("oo")) {
            return str.replace("oo", "");
        }if (str.contains("pp")) {
            return str.replace("pp", "");
        }
        if (str.contains("qq")) {
            return str.replace("qq", "");
        }
        if (str.contains("ll")) {
            return str.replace("ll", "");
        }if (str.contains("ss")) {
            return str.replace("ss", "");
        }if (str.contains("tt")) {
            return str.replace("tt", "");
        }
        if (str.contains("uu")) {
            return str.replace("uu", "");
        }
        if (str.contains("vv")) {
            return str.replace("vv", "");
        }
        if (str.contains("ww")) {
            return str.replace("ww", "");
        }if (str.contains("xx")) {
            return str.replace("xx", "");
        }if (str.contains("yy")) {
            return str.replace("yy", "");
        }
        if (str.contains("zz")) {
            return str.replace("zz", "");
        }
        else {
            return str;
        }
    }
}
